import React from 'react';

const AdminPage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Admin Panel</h1>
      <div style={{ marginTop: '1rem', color: '#aaa' }}>
        [Admin controls for managing global settings, toggles, and overrides]
      </div>
    </div>
  );
};

export default AdminPage;
